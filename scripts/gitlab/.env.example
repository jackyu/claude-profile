# GitLab API 憑證範本
# 複製本檔為同目錄的 .env，並填入你自己的真實值。
# .env 已被根目錄 .gitignore 排除，切勿 commit（含真實 token）。

# GitLab Personal Access Token（scope 需含 api）
GITLAB_PERSONAL_ACCESS_TOKEN=glpat-your-token-here

# GitLab API base URL（換成你的 GitLab 實例）
GITLAB_API_URL=https://gitlab.example.com/api/v4
